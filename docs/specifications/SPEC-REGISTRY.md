# Specification Registry

| ID | Title | Version | Design | Implementation | Verification | Depends on | Owner | Location | Evidence |
|---|---|---:|---|---|---|---|---|---|---|
| `FCLONE-DETECTION-001` | Duplicate File Detection Engine (incl. opt-in perceptual image similarity) | 0.3.0 | Accepted | Implemented | Verified (functional requirements + relative/regression benchmarks + comparison vs. fclones, matched or beaten on all 4 scenarios; perceptual pass additionally verified end-to-end against real synthetic images via the CLI) | none | baileyrd | [`docs/specifications/detection/FCLONE-DETECTION-001.md`](detection/FCLONE-DETECTION-001.md) | `crates/rusty_fclone-core` test suite (incl. `perceptual` module, 11 tests) + `benches/detection.rs` + `docs/benchmarks/FCLONES-COMPARISON.md` |
| `FCLONE-ACTION-001` | Duplicate Action Layer (delete/trash/hardlink/reflink/move/copy, rule-based selection, reference-folder guardrail) | 0.7.0 | Accepted | Implemented | Verified (all functional requirements have a dedicated test) | `FCLONE-DETECTION-001` | baileyrd | [`docs/specifications/action/FCLONE-ACTION-001.md`](action/FCLONE-ACTION-001.md) | `crates/rusty_fclone-core` `action` module (16 tests) + `folder_action` module (12 tests) + `select` module (10 tests) + `crates/rusty_fclone-cli` `main` module (29 tests) |
| `CLI-UX-001` | CLI Output, Progress, and Confirmation (incl. `history` query subcommand, opt-in perceptual similarity) | 0.3.6 | Accepted | Implemented | Verified (functional requirements have a dedicated test or documented manual smoke test) | `FCLONE-DETECTION-001`, `FCLONE-ACTION-001` | baileyrd | [`docs/specifications/cli-ux/CLI-UX-001.md`](cli-ux/CLI-UX-001.md) | `crates/rusty_fclone-cli` `main` module (32 tests) + `history` module (11 tests) |
| `GUI-UX-001` | Desktop GUI (Tauri) | 0.4.1 | Accepted | Implemented | Implemented and manually re-verified via a real Xvfb/`xdotool` pass (2026-08-27, `xdotool` newly installed in this environment for it): FR-026 (media preview, photo path only), FR-028 ("Similar content" wiring), and FR-029 (storage-breakdown chart upgrade) all confirmed rendering and behaving correctly against real scan data; FR-027 (saved scan profiles) confirmed rendering correctly, though the save/load/delete interaction itself wasn't exercised. That same pass found and fixed a real layout bug predating it (`.scan-layout`'s stale `flex: 1`, causing Scan Setup's side-column cards to overflow onto the cards below — see 0.4.1's change history entry, no ADR needed). FR-020 through FR-025 still have IPC-level coverage only, no manual UI verification; FR-025 (history export/import buttons) has no automated coverage at all — no backend command to test for the export half, and the import half is an intentionally-disabled placeholder; FR-027's `profiles::default_profiles_dir()` real-directory resolution is untested by design (would touch the host's actual config directory) and was instead verified once by hand | `FCLONE-DETECTION-001`, `FCLONE-ACTION-001` | baileyrd | [`docs/specifications/gui-ux/GUI-UX-001.md`](gui-ux/GUI-UX-001.md) | `crates/rusty_fclone-gui` `payload`/`commands`/`preview`/`profiles` modules (59 tests) |
