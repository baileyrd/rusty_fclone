# Traceability

| Requirement | Roadmap | Decision/interface | Implementation | Verification | PR/release | State |
|---|---|---|---|---|---|---|
| `FCLONE-DETECTION-001-FR-001` | `DETECTION-BASELINE` | ADR-0003 | `traversal::traverse` | `traversal::tests::finds_regular_files_only` | initial commit | Implemented, Verified |
| `FCLONE-DETECTION-001-FR-002` | `DETECTION-BASELINE` | ADR-0001 | `pipeline::run_scan` (`by_size`) | `pipeline::tests::unique_files_produce_no_groups` | initial commit | Implemented, Verified |
| `FCLONE-DETECTION-001-FR-003` | `DETECTION-BASELINE` | ADR-0001, ADR-0007 | `pipeline::process_size_group` | `pipeline::tests::finds_duplicates_larger_than_sample_size`, `pipeline::tests::no_duplicates_when_only_prefix_matches` | initial commit; sample-size decoupling in adaptive-sample-size commit | Implemented, Verified |
| `FCLONE-DETECTION-001-FR-004` | `DETECTION-BASELINE` | ADR-0003 | `traversal::traverse` (`follow_links`) | `traversal::tests::skips_symlinks_by_default` | initial commit | Implemented, Verified |
| `FCLONE-DETECTION-001-FR-005` | `DETECTION-BASELINE` | ADR-0003 | `traversal::is_excluded_by_filesystem_boundary`, `traversal::device_component` | `traversal::tests::filesystem_boundary_*` (4 cases), `traversal::tests::device_component_*` (2 cases) | gap-closure commit | Implemented, Verified |
| `FCLONE-DETECTION-001-FR-006` | `DETECTION-BASELINE` | ADR-0001, ADR-0003 | `pipeline::run_scan` (`by_file_id`) | `pipeline::tests::hardlink_aliases_are_included_when_content_matches_another_file`, `pipeline::tests::standalone_hardlink_pair_is_not_reported_as_duplicate` | gap-closure commit | Implemented, Verified |
| `FCLONE-DETECTION-001-FR-007` | `DETECTION-BASELINE` | ADR-0004 | `pipeline::process_size_group` (`filter(len > 1)`) | `pipeline::tests::unique_files_produce_no_groups`, `pipeline::tests::standalone_hardlink_pair_is_not_reported_as_duplicate` | initial commit | Implemented, Verified |
| `FCLONE-DETECTION-001-FR-008` | `DETECTION-BASELINE` | ADR-0001 | `pipeline::verify_representatives` | `pipeline::tests::verify_representatives_drops_entries_that_do_not_byte_match`, `pipeline::tests::verify_matches_true_still_reports_real_duplicates` | gap-closure commit | Implemented, Verified |
| `FCLONE-DETECTION-001-FR-009` | `DETECTION-BASELINE` | ADR-0004 | `traversal::traverse` (`on_error`), `pipeline::process_size_group`/`verify_representatives` (`report_error`) | `traversal::tests::traversal_errors_are_reported_and_do_not_abort_the_scan`, `pipeline::tests::read_failures_during_hashing_are_reported_and_do_not_abort_the_group` | gap-closure commit | Implemented, Verified — hashing-stage errors were silently dropped until the gap-closure commit; see ADR-0004 change note |
| `FCLONE-DETECTION-001-NFR-001` | `DETECTION-BASELINE` | ADR-0004 | `pipeline::ScanHandle` (`Iterator`) | `pipeline::tests::finished_event_is_always_last_and_reports_every_group` (contract-level: `Finished` is always terminal and every group precedes it), manual CLI smoke test | gap-closure commit | Implemented, Verified (contract only — no test measures wall-clock overlap between traversal and hashing; see roadmap's `DETECTION-STREAMING-OVERLAP`) |
| `FCLONE-DETECTION-001-NFR-002` | `DETECTION-BASELINE` | ADR-0001 | `pipeline::process_size_group` | `pipeline::tests::no_duplicates_when_only_prefix_matches` (indirect) | initial commit | Implemented |
| `FCLONE-DETECTION-001-NFR-003` | `DETECTION-BASELINE` | ADR-0002, ADR-0008 | `io_pool::IoPool` + rayon `into_par_iter` | `io_pool::tests::*` (pool mechanics only); default sizing validated by `docs/benchmarks/FCLONES-COMPARISON.md`'s `--io-threads` sweep | initial commit; default changed in io-thread-sizing commit | Implemented, benchmarked |
| `FCLONE-ACTION-001-FR-001` | `ACTION-LAYER` | ADR-0009 | `action::plan` | `action::tests::plans_every_non_kept_path`, `action::tests::plan_skips_existing_hardlink_aliases_of_kept` | action-layer commit | Implemented, Verified |
| `FCLONE-ACTION-001-FR-002` | `ACTION-LAYER` | ADR-0009 | `action::plan` (no `fs::` mutation calls) | `action::tests::plans_every_non_kept_path` (implicitly: files still exist and are unmodified after `plan` alone) | action-layer commit | Implemented, Verified |
| `FCLONE-ACTION-001-FR-003` | `ACTION-LAYER` | ADR-0009 | `action::apply` (`ActionKind::Delete`) | `action::tests::apply_delete_removes_redundant_copies_and_keeps_the_kept_file` | action-layer commit | Implemented, Verified |
| `FCLONE-ACTION-001-FR-004` | `ACTION-LAYER` | ADR-0009 | `action::hardlink_over` | `action::tests::apply_hardlink_replaces_redundant_copy_and_preserves_its_path` | action-layer commit | Implemented, Verified |
| `FCLONE-ACTION-001-FR-005` | `ACTION-LAYER` | ADR-0004 (error-tolerance contract), ADR-0009 | `action::apply` | `action::tests::apply_reports_per_file_failure_without_aborting_the_rest` | action-layer commit | Implemented, Verified |
| `FCLONE-ACTION-001-FR-006` | `ACTION-LAYER` | ADR-0009 | `rusty_fclone-cli::run` (`--apply` gate in `handle_group`) | `main::tests::action_without_apply_is_a_dry_run`, `main::tests::action_with_apply_actually_deletes`, `main::tests::action_with_apply_actually_hardlinks` | action-layer commit | Implemented, Verified |
| `FCLONE-ACTION-001-FR-007` | `ACTION-LAYER` | ADR-0009 | `rusty_fclone-cli::run` (`Action::Report` → `action_kind: None`) | `main::tests::default_report_action_leaves_files_untouched` | action-layer commit | Implemented, Verified |
| `FCLONE-ACTION-001-FR-008` | `ACTION-REFLINK` | ADR-0009, ADR-0014 | `action::reflink_over` | `action::tests::apply_reflink_succeeds_or_fails_cleanly_depending_on_filesystem_support` (tolerant of both outcomes — filesystem support varies) | reflink commit | Implemented, Verified — manual CLI smoke test additionally confirmed the failure path on this environment's non-CoW filesystem |
| `FCLONE-ACTION-001-FR-009` through `FR-011` | `FOLDER-ACTION` | ADR-0009, ADR-0021, ADR-0023 | `folder_action::{plan_folder, apply_folder}` | `folder_action::tests::*` (7 tests: pairing, missing-partner rejection, stale-size rejection, nonexistent-`removed` rejection, delete-with-prune, hardlink-without-prune, per-file-failure-without-prune) | folder-delete-action commit | Implemented, Verified — core only, no CLI/GUI caller yet |
| `CLI-UX-001-FR-001` through `FR-005` (JSON output shapes) | `CLI-UX` | ADR-0004, ADR-0015 | `rusty_fclone-cli::{JsonEvent, JsonAction, print_group_json, report_progress, report_error, report_finished, report_action_summary}` | `main::tests::json_format_reports_duplicates_as_ndjson` (smoke-level); manual CLI smoke test inspecting exact NDJSON shape | cli-ux commit | Implemented — no automated test asserts on exact JSON field-by-field (see `CLI-UX-001`'s open questions) |
| `CLI-UX-001-FR-006` | `CLI-UX` | ADR-0004, ADR-0015 | `model::ScanEvent::Progress`, `pipeline::run_scan` (`PROGRESS_INTERVAL_FILES`) | compile-time (CLI's exhaustive `match ScanEvent` requires a `Progress` arm); manual smoke test confirmed checkpoints at the expected 256-file interval in `--format json` on a 600-file tree | cli-ux commit | Implemented, Verified |
| `CLI-UX-001-FR-007` | `CLI-UX` | ADR-0015 | `rusty_fclone-cli::ProgressLine` (`std::io::IsTerminal`-gated) | manual smoke test confirmed zero `\r`/progress output with a piped (non-terminal) stderr | cli-ux commit | Implemented — no automated test (would need a pseudo-terminal harness) |
| `CLI-UX-001-FR-008`/`FR-009` | `CLI-UX` | ADR-0009, ADR-0015 | `rusty_fclone-cli::{confirm, confirm_apply}` | `main::tests::confirm_accepts_y_and_yes_case_insensitively`, `main::tests::confirm_rejects_anything_else`, `main::tests::apply_without_yes_is_blocked_by_the_unanswered_confirmation_prompt` | cli-ux commit | Implemented, Verified — accept path (`echo y \|`) additionally confirmed by manual smoke test |
| `FCLONE-DETECTION-001-NFR-004` | `DETECTION-INCREMENTAL-CACHE` | ADR-0016 | `cache::HashCache`, `pipeline::process_size_group` (full-hash stage) | `cache::tests::*` (7 tests: encode/decode round-trip, miss on new cache, hit/miss on size/mtime match, reopen preserves entries, overwrite); `pipeline::tests::cached_scan_produces_identical_results_to_an_uncached_one`, `pipeline::tests::a_changed_file_is_not_served_a_stale_cached_hash` | redb-cache commit | Implemented, Verified — manual CLI smoke test additionally confirmed zero hits on a cold run and exactly one hit per file on an immediately-following warm run, via `-vvv` trace output |
| `CLI-UX-001-FR-010`/`FR-011` | `CLI-SCAN-HISTORY` | ADR-0017 | `rusty_fclone-cli::history::{ScanRecord, record_scan}` | `history::tests::*` (4 tests: schema creation, every field recorded, multiple scans append, reopen preserves prior rows); `main::tests::history_flag_records_one_row_per_scan`, `main::tests::history_flag_records_the_action_result_when_an_action_runs` | sqlite-history commit | Implemented, Verified — manual CLI smoke test additionally confirmed two real scans (plain, then `--action delete --apply`) produced two correctly-populated rows via a direct SQL query |
| `FCLONE-DETECTION-001-NFR-005` | `DETECTION-FCLONES-CACHE-IMPORT` | ADR-0019 | `fclones_import::FclonesImportCache`, `pipeline::process_size_group` (full-hash stage, after a `--cache` miss) | `fclones_import::tests::*` (9 tests: hash decode against a real captured fclones report value, exact-match hit, small-file default-prefix-length hit, non-default-prefix-size miss, missing-tree miss, nonexistent-path miss, size/mtime staleness misses, partial-hash-not-full-hash miss) | fclones-cache-import commit | Implemented, Verified — additionally exercised end-to-end against the real `fclones` 0.35.0 binary in this development environment (both the exact-match and default-prefix-length paths), not just hand-built fixtures |
| `GUI-UX-001-FR-001`/`FR-002` | `GUI` | ADR-0004, ADR-0020 | `rusty_fclone-gui::commands::start_scan` | `commands::tests::start_scan_accepts_a_valid_root_and_returns_immediately`; manual Xvfb/`xdotool` end-to-end pass | gui commit | Implemented, Verified |
| `GUI-UX-001-FR-003` through `FR-006` | `GUI` | ADR-0004, ADR-0015, ADR-0020 | `rusty_fclone-gui::payload::ScanEventPayload` (and its `From` impls) | `payload::tests::scan_event_payload_serializes_with_a_snake_case_type_tag`, `payload::tests::duplicate_group_converts_to_a_scan_event_payload_with_display_paths`; manual end-to-end pass confirmed real emitted events rendered correctly | gui commit | Implemented, Verified |
| `GUI-UX-001-FR-007` | `GUI` | ADR-0020 | `rusty_fclone-gui::payload::ScanOptionsPayload` | `payload::tests::scan_options_payload_applies_defaults_for_omitted_fields` | gui commit | Implemented, Verified |
| `GUI-UX-001-FR-008` through `FR-010` | `GUI` | ADR-0009, ADR-0020 | `rusty_fclone-gui::commands::run_action` | `commands::tests::run_action_delete_preview_does_not_touch_the_filesystem`, `commands::tests::run_action_delete_apply_removes_the_redundant_copy`, `commands::tests::run_action_rejects_an_unknown_kind` (all IPC-level via `tauri::test`, asserting real filesystem state); manual end-to-end pass additionally confirmed a real delete through the rendered UI | gui commit | Implemented, Verified |
| `GUI-UX-001-FR-011` | `GUI`, `GUI-REDESIGN` | ADR-0009, ADR-0020, ADR-0022 | `ui/app.js` (`applyAction`'s `window.confirm` gate before `run_action`) | Manual end-to-end pass only (dialog text checked, screenshotted, real delete confirmed before/after) — no automated DOM test exists | gui-design-redesign commit | Implemented — no automated test (see `GUI-UX-001`'s open questions); mechanism revised in `GUI-REDESIGN` from an apply checkbox to a confirmation dialog, same underlying requirement |
| `GUI-UX-001-FR-012` | `GUI` | ADR-0020 | `rusty_fclone-gui::payload::normalize_path_input`, used by `commands::start_scan` and `ScanOptionsPayload`'s `From` impl | `payload::tests::normalize_path_input_*` (5 tests), `payload::tests::scan_options_payload_normalizes_pasted_quoted_paths_too`, `commands::tests::start_scan_treats_a_windows_copy_as_path_quoted_root_as_the_real_directory` (IPC-level, verified to fail without the fix and pass with it) | gui-quote-fix commit | Implemented, Verified — found via a real Windows user pasting an Explorer "Copy as path" value |
| `GUI-UX-001-FR-013` | `DETECTION-FOLDER-DEDUP` | ADR-0021 | `rusty_fclone-gui::commands::find_duplicate_folders`, `payload::FolderMatchPayload` | `commands::tests::find_duplicate_folders_reports_a_contained_folder_match`, `commands::tests::find_duplicate_folders_rejects_a_nonexistent_root`, `payload::tests::folder_match_exact_serializes_with_a_snake_case_type_tag_and_camel_case_fields`, `payload::tests::folder_match_contained_serializes_with_subset_and_superset_paths`; manual end-to-end pass confirmed the frontend (`GUI-REDESIGN`) calls it automatically and renders both match kinds correctly | gui-folder-dedup-command commit | Implemented, Verified — frontend caller landed in `GUI-REDESIGN` (FR-015) |
| `GUI-UX-001-FR-014` through `FR-017` | `GUI-REDESIGN` | ADR-0022 | `ui/app.js` (state, `render()`, per-screen render functions, `applyAction`, `onScanFinished`), `ui/style.css`, `ui/icons.js` | Manual end-to-end pass: Dashboard/Scan Setup/Duplicate Review driven by a real scan against a tempdir with both file- and folder-level duplicates; keep-choice reordering confirmed via a real delete leaving the chosen path and removing the other; file-type chips confirmed filtering the Review list only (scan itself unaffected); folder-dedup pass confirmed auto-triggered after `Finished` | gui-design-redesign commit | Implemented — no automated DOM test exists (see `GUI-UX-001`'s open questions) |
| `GUI-UX-001-FR-018`/`FR-019` | `FOLDER-ACTION` | ADR-0023 | `rusty_fclone-gui::commands::run_folder_action`, `payload::{FolderActionPlanPayload, FolderApplyReportPayload, FolderActionResultPayload}`, `ui/app.js` (`folderMatchPairs`, `folderReviewMain`, `applyFolderAction`) | `commands::tests::run_folder_action_delete_preview_does_not_touch_the_filesystem`, `commands::tests::run_folder_action_delete_apply_removes_the_folder`, `commands::tests::run_folder_action_rejects_a_stale_scan`, `payload::tests::folder_action_plan_converts_with_camel_case_fields`; manual end-to-end pass against two real trees confirmed the enabled "Delete Duplicate Folder" button, a `Contained` match's real folder prune, and an `Exact` match's keep-choice badge actually driving which folder was removed vs. kept | folder-action-gui commit | Implemented, Verified — closes the gap `GUI-REDESIGN` left open (the button shipped disabled pending ADR-0023) |
| `FCLONE-DETECTION-001-FR-010` through `FR-013`, `NFR-006` | `DETECTION-FOLDER-DEDUP` | ADR-0021 | `rusty_fclone_core::folder_dedup::{find_folder_duplicates, build_tree, compute_all_signatures, find_supersets}` | `folder_dedup::tests::*` (7 tests: exact match, unmatched-file disqualification incl. still-eligible-as-superset, contained subset/superset, nested-match redundancy suppression, independent nested match still found, no groups → no matches, nonexistent root rejected) | folder-dedup commit | Implemented, Verified |
| `CLI-UX-001-FR-012` | `DETECTION-FOLDER-DEDUP` | ADR-0021 | `rusty_fclone-cli::{report_folder_matches, print_folder_match_text, print_folder_match_json}` | `main::tests::find_duplicate_folders_flag_succeeds_on_an_exact_folder_match`, `main::tests::find_duplicate_folders_flag_succeeds_with_json_format`; manual CLI smoke test against a real three-directory tree confirmed exact text/NDJSON shapes for both `Exact` and `Contained` | folder-dedup commit | Implemented, Verified |
| `CLI-UX-001-FR-013` | `FOLDER-ACTION` | ADR-0009, ADR-0021, ADR-0023 | `rusty_fclone-cli::{report_folder_matches, folder_match_pairs, folder_match_roots, group_fully_covered_by, print_folder_match_text, print_folder_match_json}`, `folder_action::{plan_folder, apply_folder}` | `main::tests::find_duplicate_folders_with_action_delete_apply_removes_the_subset_folder`, `main::tests::find_duplicate_folders_with_action_delete_without_apply_is_a_dry_run`, `main::tests::find_duplicate_folders_with_action_still_acts_on_an_unrelated_duplicate_pair`; manual CLI smoke test against a real filesystem confirmed preview mode, real `--apply` folder pruning (subset removed, superset untouched, confirmed via `find`), and exact JSON field shapes | folder-action-cli commit | Implemented, Verified — found and fixed a real ordering bug during implementation: live per-group action inside the streaming scan loop was consuming the file evidence the post-scan folder-dedup pass needed, silently making folder matches vanish; fixed by deferring all individual-group reporting/action until after the folder-dedup pass whenever `--find-duplicate-folders` is set |

| `FCLONE-DETECTION-001-FR-014`, `NFR-007` | `DETECTION-SCAN-FILTERS` | none (routine implementation, no ADR) | `traversal::{size_allowed, extension_allowed}`, `traversal::traverse` (`process_read_dir` subtree pruning) | `traversal::tests::size_allowed_*` (2), `traversal::tests::extension_allowed_*` (3), `traversal::tests::min_size_filter_skips_small_files_during_a_real_traversal`, `traversal::tests::extension_filter_skips_non_matching_files_during_a_real_traversal`, `traversal::tests::exclude_paths_prunes_the_whole_subtree_not_just_matching_files`, `traversal::tests::exclude_paths_can_target_a_single_file` | detection-scan-filters commit | Implemented, Verified |
| `CLI-UX-001-FR-014` | `DETECTION-SCAN-FILTERS` | none | `rusty_fclone-cli::Cli` (`--min-size`/`--max-size`/`--include-ext`/`--exclude-ext`/`--exclude-path`) | Exercised transitively by every existing CLI test via `base_cli`'s defaults (all `None`/empty, so behavior is unchanged when unset); no new CLI-level test dedicated to a non-default filter value yet | detection-scan-filters commit | Implemented — no dedicated CLI-level test for a non-default filter value (same standing pattern as `--cache`/`--io-threads`, neither of which has one either); see `PROJECT-STATUS.md`'s Risks section |
| `GUI-UX-001-FR-020` | `DETECTION-SCAN-FILTERS` | none | `ui/app.js` (Scan Setup's "Include/exclude filters" card, `scanOptionsPayload`), `rusty_fclone-gui::payload::ScanOptionsPayload`/`From` impl | `payload::tests::scan_options_payload_passes_through_filters_and_normalizes_exclude_paths` | detection-scan-filters commit | Implemented — no automated DOM test exists (same standing gap as the rest of `app.js`, see `GUI-UX-001`'s open questions) |

| `FCLONE-ACTION-001-FR-001` (revised), `FR-011` (revised), `FR-012` | `ACTION-TRASH` | ADR-0009, ADR-0024 | `action::apply` (`ActionKind::Trash` arm, `trash::delete`), `folder_action::apply_folder` (`prunes_directory` gate) | `action::tests::apply_trash_removes_the_redundant_copy_from_its_original_path_and_keeps_the_kept_file`, `folder_action::tests::apply_folder_trash_removes_every_file_and_prunes_the_empty_directory` | action-trash commit | Implemented, Verified — additionally confirmed end-to-end in this environment via a real file moved to `~/.local/share/Trash/files/`, recoverable |
| `CLI-UX-001` (`--action trash`, no new FR — reuses the existing generic `--action <ACTION>` flag documented against `FCLONE-ACTION-001`) | `ACTION-TRASH` | ADR-0024 | `rusty_fclone-cli::{Action::Trash, action_word}` | `main::tests::action_with_apply_actually_trashes`; manual CLI smoke test against a real filesystem (file-level and `--find-duplicate-folders` combined) confirming the file/folder actually moved to the OS trash and the kept side untouched | action-trash commit | Implemented, Verified |
| `GUI-UX-001-FR-008` (revised), `FR-018` (revised), `FR-021` | `ACTION-TRASH` | ADR-0024 | `rusty_fclone-gui::payload::parse_action_kind` (`"trash"` branch), `ui/app.js` (`ACTION_KINDS`, default `actionKind: "trash"`, `actionVerb` maps) | `payload::tests::parse_action_kind_accepts_the_four_known_words_and_rejects_others` | action-trash commit | Implemented — no IPC-level or manual Xvfb/`xdotool` verification yet for the new action kind through the rendered UI (same standing gap `GUI-UX-001`'s open questions already track for `app.js`) |

| `FCLONE-ACTION-001-FR-013`, `FR-014` | `SELECTION-RULES` | none (routine implementation, no ADR) | `action::plan_with_keep`, `select::{Rule, choose_keep, by_modified, by_depth}` | `action::tests::plan_with_keep_honors_an_explicit_non_default_kept_path`; `select::tests::*` (8 tests: alphabetically-first, newest, oldest, metadata-unreadable fallback, shortest/longest path, tie-breaking across all four non-default rules) | selection-rules commit | Implemented, Verified |
| `CLI-UX-001-FR-015` | `SELECTION-RULES` | none | `rusty_fclone-cli::{KeepRule, handle_group}` | `main::tests::keep_rule_newest_keeps_the_most_recently_modified_file`; manual CLI smoke test against a real two-file tree confirming `--keep-rule newest` picks the newer file and both text and `--format json` show the correct reason | selection-rules commit | Implemented, Verified |
| `GUI-UX-001-FR-008` (revised), `FR-009` (revised), `FR-014` (revised), `FR-022` | `SELECTION-RULES` | none | `rusty_fclone-gui::commands::choose_keep`, `payload::{parse_keep_rule, ChooseKeepPayload}`, `ui/app.js` (`ensureRuleKeepChoice`, `rulesView`'s real "Keep newest copy" toggle) | `commands::tests::choose_keep_resolves_the_newest_file_without_touching_the_filesystem`, `commands::tests::choose_keep_rejects_an_unknown_rule`, `commands::tests::run_action_keep_reason_defaults_to_a_placeholder_when_omitted`, `commands::tests::run_action_passes_through_an_explicit_keep_reason`, `payload::tests::parse_keep_rule_accepts_the_five_known_words_and_rejects_others` | selection-rules commit | Implemented, IPC-level tests pass — no manual Xvfb/`xdotool` verification through the rendered UI yet (same standing gap `DETECTION-SCAN-FILTERS` and `ACTION-TRASH` already left open for their own GUI surfaces) |

| `FCLONE-ACTION-001-FR-015` through `FR-017` | `ACTION-REFERENCE-FOLDERS` | ADR-0009, ADR-0025 | `select::{choose_keep, is_protected, protected_member}`, `action::{plan, plan_with_keep}`, `folder_action::{plan_folder, apply_folder}` (`protected_files_skipped` field/prune gate) | `select::tests::*` (protected path wins over every `Rule`, prefix matching, empty list → `None`), `action::tests::plan_overrides_an_explicit_keep_when_a_different_path_is_protected`, `action::tests::plan_never_includes_a_protected_path_in_actions_even_when_it_is_not_kept`, `action::tests::apply_never_touches_a_protected_path`, `folder_action::tests::plan_folder_excludes_a_protected_file_from_pairs_and_counts_it_skipped`, `folder_action::tests::apply_folder_never_prunes_the_directory_when_a_protected_file_was_skipped` | action-reference-folders commit | Implemented, Verified |
| `CLI-UX-001-FR-016` | `ACTION-REFERENCE-FOLDERS` | ADR-0009, ADR-0025 | `rusty_fclone-cli::{Cli::reference_paths, handle_group, report_folder_matches}` | `main::tests::reference_path_overrides_keep_rule_and_is_never_acted_on`, `main::tests::find_duplicate_folders_with_reference_protects_a_file_and_blocks_the_prune`; manual CLI smoke test against real filesystems confirmed both the file-level and folder-level guarantee (protected file survives, subset directory not pruned) | action-reference-folders commit | Implemented, Verified |
| `GUI-UX-001-FR-023` | `ACTION-REFERENCE-FOLDERS` | ADR-0009, ADR-0025 | `rusty_fclone-gui::commands::{run_action, choose_keep, run_folder_action}` (`reference_paths` param), `payload::normalize_path_list`, `ui/app.js` (`state.referencePaths`, `referencePathsList`, `ensureRuleKeepChoice`'s alphabetical-with-guardrail case) | `commands::tests::run_action_reference_path_overrides_the_chosen_keep_and_is_never_acted_on`, `commands::tests::run_folder_action_reference_path_protects_a_file_and_blocks_the_prune` (both IPC-level, real filesystem state) | action-reference-folders commit | Implemented, IPC-level tests pass — no manual Xvfb/`xdotool` verification through the rendered UI yet (`xdotool` isn't installed in this environment; same standing gap `DETECTION-SCAN-FILTERS`/`ACTION-TRASH`/`SELECTION-RULES` already left open for their own GUI surfaces) |

| `FCLONE-ACTION-001-FR-018`, `FR-019` | `ACTION-MOVE-COPY` | ADR-0009, ADR-0014, ADR-0026 | `action::{ActionKind::Move, ActionKind::Copy, archived_path, move_into, copy_into}`, `folder_action::apply_folder` (`prunes_directory` gate extended to `Move`) | `action::tests::apply_move_relocates_the_redundant_copy_into_the_archive_directory`, `action::tests::apply_copy_leaves_the_original_in_place_and_reclaims_nothing`, `action::tests::apply_move_fails_without_clobbering_an_existing_archive_destination`, `action::tests::archived_path_mirrors_original_structure_so_same_named_files_never_collide`, `folder_action::tests::apply_folder_move_relocates_every_file_and_prunes_the_empty_directory`, `folder_action::tests::apply_folder_copy_leaves_every_file_and_the_directory_in_place` | action-move-copy commit | Implemented, Verified |
| `CLI-UX-001-FR-017` | `ACTION-MOVE-COPY` | ADR-0009, ADR-0026 | `rusty_fclone-cli::{Action::Move, Action::Copy, Cli::archive_dir, Action::as_core_kind}` | `main::tests::action_with_apply_actually_moves_into_the_archive_directory`, `main::tests::action_with_apply_actually_copies_into_the_archive_directory_and_keeps_the_original`, `main::tests::action_move_without_archive_dir_fails_before_touching_anything`; manual CLI smoke test against real filesystems confirmed `move`'s relocation, `copy`'s leave-original-in-place + zero-bytes-reclaimed behavior, the never-clobber guard on a repeated `copy` run, and the `--archive-dir`-required rejection | action-move-copy commit | Implemented, Verified |
| `GUI-UX-001-FR-024` | `ACTION-MOVE-COPY` | ADR-0009, ADR-0026 | `rusty_fclone-gui::commands::{run_action, run_folder_action}` (`archive_dir` param), `payload::parse_action_kind` (`"move"`/`"copy"` branches), `ui/app.js` (`ACTION_KINDS`, `ARCHIVE_KINDS`, `state.archiveDir`, conditional archive-folder field, `"copy"`-specific reclaim text) | `commands::tests::run_action_move_relocates_the_redundant_copy_into_the_archive_directory`, `commands::tests::run_action_copy_leaves_the_original_in_place`, `commands::tests::run_action_move_without_archive_dir_is_rejected`, `commands::tests::run_folder_action_move_relocates_every_file_and_prunes_the_folder`, `payload::tests::parse_action_kind_move_and_copy_require_an_archive_directory` (all IPC-level, real filesystem state) | action-move-copy commit | Implemented, IPC-level tests pass — no manual Xvfb/`xdotool` verification through the rendered UI yet (same standing gap `ACTION-REFERENCE-FOLDERS` and earlier units already left open) |

| `CLI-UX-001-FR-018` | `CLI-HISTORY-AUDIT` | ADR-0017, ADR-0027 | `rusty_fclone-cli::history::{ActionRecord, ScanRow, HistoryStats, list_scans, stats}`, `rusty_fclone-cli::{record_action_outcomes, HistoryCli, HistoryCommand, run_history}`, `main` (`args[1] == "history"` pre-dispatch) | `history::tests::*` (11 tests: action-row recording, correct scan correlation, `list_scans`/`stats` behavior incl. empty-database cases), `main::tests::history_flag_records_one_action_row_per_file_acted_on`, `history_flag_records_no_action_rows_for_a_dry_run`, `history_flag_records_action_rows_for_folder_level_actions`, `history_list_reports_recent_scans_newest_first`, `history_stats_aggregates_across_the_database`, `history_subcommand_reports_failure_for_an_unreadable_database`; manual CLI smoke test against a real filesystem confirmed two real scans, `history list`/`--format json`/`stats` output, and that a plain scan invocation is unaffected by the new keyword dispatch | cli-history-audit commit | Implemented, Verified |
| `GUI-UX-001-FR-025` | `CLI-HISTORY-AUDIT` | ADR-0027 | `ui/app.js` (`exportScanHistoryJson`, "Import history" button's updated disabled-reason tooltip) | No automated coverage — `app.js` has no JS test harness in this project (same standing gap as the rest of the frontend); no manual Xvfb/`xdotool` pass this session | cli-history-audit commit | Implemented — no automated or manual UI verification yet, same standing gap as every other GUI surface this session |

| `GUI-UX-001-FR-026` | `GUI-MEDIA-PREVIEW` | ADR-0028 | `rusty_fclone-gui::commands::read_preview`, `preview::{build_data_url, mime_for_extension, base64_encode}`, `ui/app.js` (`ensurePreview`, `PREVIEWABLE_CATEGORIES`, `fileReviewMain`'s per-card `<img>`/`<audio>` rendering) | `preview::tests::*` (7 tests: base64 against RFC 4648 vectors, extension→MIME mapping incl. HEIC/TIFF/video exclusions, size-cap boundary, real I/O error), `commands::tests::read_preview_returns_a_data_url_for_a_supported_image`, `commands::tests::read_preview_rejects_an_unsupported_extension`, `commands::tests::read_preview_rejects_a_nonexistent_file` (IPC-level); base64 additionally round-tripped byte-for-byte against a real 69-byte PNG file outside the unit-test suite | gui-media-preview commit | Implemented, IPC-level tests pass — no manual Xvfb/`xdotool` verification through the rendered UI yet (same standing gap every GUI-facing unit this session has carried) |
| `GUI-UX-001-FR-027` | `SCAN-PROFILES` | ADR-0029 | `rusty_fclone-gui::commands::{list_scan_profiles, save_scan_profile, delete_scan_profile}`, `profiles::{default_profiles_dir, load, upsert, remove}`, `ui/app.js` (`profilesCard`, `loadScanProfiles`, `saveScanProfile`, `applyScanProfile`, `deleteScanProfile`) | `profiles::tests::*` (7 tests: empty-when-missing, insert, overwrite-by-name, preserves other profiles, remove, no-op remove of an unknown name, options round trip through the saved JSON file — all hermetic against a tempdir), `commands::tests::save_scan_profile_rejects_an_empty_name`, `commands::tests::save_scan_profile_rejects_a_whitespace_only_name` (IPC-level); `default_profiles_dir()`'s real resolution verified once by hand (resolved to `/root/.config/rusty-fclone` in this environment, a saved/reloaded profile matched the expected JSON exactly) rather than by an automated test, since exercising it for real would touch the host's actual config directory | scan-profiles commit | Implemented, hermetic unit/IPC tests pass — no manual Xvfb/`xdotool` verification through the rendered UI yet (same standing gap every GUI-facing unit this session has carried) |

| `FCLONE-DETECTION-001-FR-015` through `FR-017`, `NFR-008` | `DETECTION-PERCEPTUAL-IMAGES` | ADR-0030 | `rusty_fclone_core::perceptual::{find_similar_images, dhash, hamming_distance, cluster}` | `perceptual::tests::*` (11 tests: dHash against identical/very-different/lightly-perturbed images, Hamming distance, union-find clustering threshold/singleton behavior, `find_similar_images` end-to-end against real files on disk incl. non-image and bogus-content skipping, nonexistent-root rejection); manually verified end-to-end against real synthetic JPEG/PNG photos via the compiled CLI binary, confirming correct clustering while the exact engine simultaneously reported zero duplicate groups for the same tree | detection-perceptual-images commit | Implemented, Verified |
| `CLI-UX-001-FR-019`, `FR-020` | `DETECTION-PERCEPTUAL-IMAGES` | ADR-0030 | `rusty_fclone-cli::{Cli::find_similar_images, Cli::similarity_threshold, report_similar_group}`, `JsonEvent::SimilarImages` | `main::tests::find_similar_images_flag_succeeds_and_does_not_touch_the_filesystem`, `find_similar_images_flag_succeeds_with_json_format`, `similarity_threshold_flag_is_accepted_and_does_not_error`; manual CLI smoke test against a real four-image tree (see `FCLONE-DETECTION-001-FR-015` row) confirmed exact text/NDJSON output and correct clustering in both formats | detection-perceptual-images commit | Implemented, Verified |
| `GUI-UX-001-FR-028` | `DETECTION-PERCEPTUAL-IMAGES` | ADR-0030 | `rusty_fclone-gui::commands::find_similar_images`, `payload::SimilarGroupPayload`, `ui/app.js` (enabled "Similar content" seg-option, `onScanFinished`'s independent perceptual-pass call, `reviewItems`/`groupListRow`/`reviewMain` third item kind, `similarReviewMain`) | `commands::tests::find_similar_images_groups_a_real_near_identical_pair`, `commands::tests::find_similar_images_rejects_a_nonexistent_root` (IPC-level, real synthetic image files), `payload::tests::similar_group_converts_with_camel_case_fields` | detection-perceptual-images commit | Implemented, IPC-level tests pass — no manual Xvfb/`xdotool` verification through the rendered UI yet (same standing gap every GUI-facing unit this session has carried) |

| `GUI-UX-001-FR-029` | `DASHBOARD-CHART-UPGRADE` | none (routine implementation, no ADR) | `ui/app.js` (`storageBreakdown`'s new `bytes` field, `showChartTooltip`/`hideChartTooltip`, `el()`'s generic `on<Event>` prop handler, the Dashboard's `.breakdown-segment` buttons), `ui/style.css` (`.breakdown-segment`, `.chart-tooltip`) | No automated coverage — pure `app.js`/`style.css` change, no Rust surface (same standing gap as the rest of `app.js`); the underlying `KIND_COLOR` categorical palette was run through the `dataviz` skill's `validate_palette.js` against both theme surfaces as part of this change (see `GUI-UX-001`'s open questions for the finding) | dashboard-chart-upgrade commit | Implemented — no automated or manual UI verification yet, same standing gap as every other GUI surface this session |

State legend: `Implemented, Verified` = a requirement with a dedicated test
exercising it directly. `Implemented` (no `Verified`) = code exists and
matches the requirement but only indirect/no test coverage exists yet.

All requirements previously flagged "needs dedicated unit test" — across
both detection and action layer — are closed. Adding
`FCLONE-ACTION-001-FR-006`/`FR-007`'s tests required extracting a testable
`run(cli: Cli) -> ExitCode` from `rusty_fclone-cli`'s `main`, giving the CLI
crate its first test suite (`rusty_fclone-cli`'s `unittests src/main.rs`
previously always ran 0 tests).
