# Roadmap

| Unit | Outcome | Depends on | Specs | Exit gate | Status | Evidence |
|---|---|---|---|---|---|---|
| `DETECTION-BASELINE` | Working duplicate-detection engine + CLI, cross-platform | none | `FCLONE-DETECTION-001` | `cargo fmt`/`clippy`/`test` green; CLI finds duplicates on a real tree | Done | initial workspace commit |
| `DETECTION-BENCHMARK` | Criterion benchmark suite over synthetic trees (many small duplicates, many unique files, few large duplicates, a mixed realistic tree), reporting files/sec and bytes/sec | `DETECTION-BASELINE` | `FCLONE-DETECTION-001` | `cargo bench` exists, compiles in CI (`--no-run`), and runs manually with real numbers | Done | `crates/rusty_fclone-core/benches/detection.rs`; sample run recorded in `docs/PROJECT-STATUS.md` |
| `DETECTION-BENCHMARK-VS-FCLONES` | Head-to-head comparison against upstream fclones on the same synthetic trees, so "fastest possible" becomes a measured claim rather than an architectural intent | `DETECTION-BENCHMARK` | `FCLONE-DETECTION-001` | Documented comparison (methodology + numbers) against an installed fclones build | Done | `docs/benchmarks/FCLONES-COMPARISON.md`; `scripts/gen_bench_trees.py`, `scripts/bench-vs-fclones.sh` |
| `DETECTION-ADAPTIVE-SAMPLE-SIZE` | Decouple the partial-hash sample size from the small-file threshold so large files aren't over-sampled during pruning | `DETECTION-BENCHMARK-VS-FCLONES` | `FCLONE-DETECTION-001` (revision) | `few_large_duplicates` benchmark scenario matches or beats fclones | Done, but see evidence — this alone didn't move the target scenario | ADR-0007; new `partial_hash_sample_size` field + `--partial-hash-sample-size` flag. Re-measured after: no meaningful change on `few_large_duplicates` (every file there is a real duplicate, so nothing gets pruned regardless of sample size) — see `docs/benchmarks/FCLONES-COMPARISON.md` |
| `DETECTION-IO-THREAD-SIZING` | Size the I/O pool to core count instead of an oversubscribed multiple, after benchmarking showed oversubscription hurting throughput on every tested scenario | `DETECTION-ADAPTIVE-SAMPLE-SIZE` | `FCLONE-DETECTION-001` (revision) | `few_large_duplicates` benchmark scenario matches or beats fclones | Done | ADR-0008; `few_large_duplicates` moved from 1.21x slower to within noise of fclones' best config and beats its default; small-file scenarios improved from ~1.9-2.0x to ~2.6-2.7x faster. Exit gate met by this unit, not `DETECTION-ADAPTIVE-SAMPLE-SIZE` — see `docs/benchmarks/FCLONES-COMPARISON.md` |
| `DETECTION-TRAVERSAL-COLLAPSE-FUSION` | Traversal and hardlink-collapse run as one streaming pass — each candidate is folded into the file-id map as jwalk produces it, instead of first materializing a `Vec<Candidate>` and looping over it separately | `DETECTION-BASELINE` | `FCLONE-DETECTION-001` (revision) | `cargo fmt`/`clippy`/`test`/`bench --no-run` green; manual CLI smoke test unchanged | Done | ADR-0012; `traversal::traverse` takes an `on_candidate` callback instead of returning a `Vec`; `pipeline::run_scan` folds hardlink-collapse into that callback directly |
| `DETECTION-STREAMING-OVERLAP` | Hashing begins before traversal finishes (full pipeline overlap), per ADR-0002/ADR-0004's noted scope cut | `DETECTION-TRAVERSAL-COLLAPSE-FUSION` | `FCLONE-DETECTION-001` (revision) | Benchmark shows reduced time-to-first-result on a large tree | Not Started | `DETECTION-TRAVERSAL-COLLAPSE-FUSION` above is a narrower, already-done step toward this (one pass instead of three), deliberately stopping short of overlap: hashing still only starts once traversal fully completes, since starting it earlier would let a `DuplicateGroup` be emitted, then later revised as more of the tree is walked — breaking `ScanEvent`'s "no group revision after emission" finality contract (ADR-0004). Doing this for real needs that contract redesigned first, which is a bigger, separate decision than this unit's scope (see ADR-0012's consequences) |
| `DETECTION-DEVICE-AWARE-IO-SIZING` | Pick `io_threads`'s default from whether the scan root's storage is rotational (Linux, best-effort via `/proc/self/mountinfo` + `/sys/dev/block/*/queue/rotational`) instead of one guessed constant | `DETECTION-IO-THREAD-SIZING` | `FCLONE-DETECTION-001` (revision) | `cargo fmt`/`clippy`/`test`/`bench --no-run` green; manual CLI smoke test confirms auto-detect and explicit `--io-threads` override both work | Done | ADR-0013; `rusty_fclone_core::device::default_io_threads`; `ScanOptions::io_threads`/CLI `--io-threads` change from `usize` to `Option<usize>` (`None` = auto-detect) |
| `DETECTION-LINUX-FASTPATH` | Optional Linux-specific I/O fast path: io_uring / `FIEMAP` extent-ordered reads, behind a feature flag, without breaking the cross-platform default | `DETECTION-DEVICE-AWARE-IO-SIZING` | new spec | Benchmark shows meaningful improvement on Linux; cross-platform build unaffected | Not Started | `DETECTION-DEVICE-AWARE-IO-SIZING` above closes this unit's thread-sizing half; io_uring/`FIEMAP` needs an async runtime and unsafe FFI, deserving its own ADR — deliberately not attempted here |
| `ACTION-LAYER` | Delete/hardlink confirmed duplicates, safely | `DETECTION-BASELINE` | `FCLONE-ACTION-001` | Dry-run-by-default, two-flag confirmation, and tests for every destructive path | Done | ADR-0009; `crates/rusty_fclone-core/src/action.rs` (6 tests); `--action`/`--apply` CLI flags; manual smoke tests for delete, hardlink, and unchanged default (report) behavior |
| `ACTION-REFLINK` | Copy-on-write clone as a third `ActionKind`, alongside delete/hardlink | `ACTION-LAYER` | `FCLONE-ACTION-001` (revision) | Reflink works on at least one CFS filesystem (Btrfs/XFS/APFS) without breaking the cross-platform default elsewhere | Done, with a caveat | ADR-0014; `reflink-copy` crate (strict `reflink`, not `reflink_or_copy` — no silent copy fallback); same temp-then-rename safety pattern as `Hardlink`. Exit gate's "works on at least one CFS filesystem" is implemented (delegated to `reflink-copy`'s platform ioctl, e.g. Linux `FICLONE`) but not verified end-to-end here — this environment's filesystem isn't CoW-capable, so testing (unit + manual smoke) only exercised the graceful-failure path, confirmed clean (per-file error, no silent fallback, no stray temp file, kept file and content both correct) |
| `CLI-UX` | Richer CLI output (JSON, progress reporting, interactive confirmation prompt for actions) | `DETECTION-BASELINE`, `ACTION-LAYER` | `CLI-UX-001` | Documented output contract with tests | Done | ADR-0015; `--format text\|json` (NDJSON), new `ScanEvent::Progress` (256-file interval, terminal-gated live text line via `std::io::IsTerminal`), `-y`/`--yes`-bypassable confirmation prompt before `--apply` mutates anything |
| `DETECTION-INCREMENTAL-CACHE` | Opt-in `redb`-backed full-hash cache so re-scanning an unchanged tree skips re-reading/re-hashing files whose `(size, mtime)` haven't changed | `DETECTION-BASELINE` | `FCLONE-DETECTION-001` (revision) | `cargo fmt`/`clippy`/`test`/`bench --no-run` green; manual CLI smoke test confirms zero hits cold, hits warm, correct results either way | Done | ADR-0016; `ScanOptions::cache_path`/CLI `--cache <path>`, off by default; cache-off path structurally unaffected (a `None` short-circuit before any extra work) — benchmark confirmation was inconclusive due to this environment's noisy shared-container load, not a sign of a real regression |
| `CLI-SCAN-HISTORY` | Opt-in SQLite scan-history: one summary row per completed scan, for longer-term analytics across repeated scans | `DETECTION-BASELINE`, `ACTION-LAYER` | `CLI-UX-001` (revision) | `cargo fmt`/`clippy`/`test`/`bench --no-run` green; manual CLI smoke test confirms two real scans produce two correctly-populated rows | Done | ADR-0017; CLI `--history <path>`, off by default, CLI-crate-only (`rusqlite`, `bundled` feature); deliberately scoped to per-scan summaries, not per-file/per-group detail, and no query/report subcommand yet — both explicitly deferred |
| `RELEASE-BINARIES` | Tag-triggered GitHub Actions workflow that builds `rusty-fclone` for Linux/macOS (x86_64 + aarch64)/Windows and attaches the archives to the tag's GitHub Release | none (infrastructure, not detection/action code) | none (process/tooling, not a product spec) | Workflow runs on a `v*` tag push and via manual dispatch; `v0.1.0`'s release gains real assets when dispatched against that tag | Done, verified | ADR-0018; `.github/workflows/release.yml`; `softprops/action-gh-release` appends assets without touching an already-created release's title/body; a first real run (`workflow_dispatch` from `main` with `tag=v0.1.0`, needing the `tag`-input fix in ADR-0018's consequences) succeeded and attached all four platform archives to `v0.1.0`'s release: `rusty-fclone-v0.1.0-x86_64-unknown-linux-gnu.tar.gz` (2,444,918 bytes), `rusty-fclone-v0.1.0-aarch64-apple-darwin.tar.gz` (2,161,166 bytes), `rusty-fclone-v0.1.0-x86_64-apple-darwin.tar.gz` (2,310,563 bytes), `rusty-fclone-v0.1.0-x86_64-pc-windows-msvc.zip` (2,246,772 bytes) |
| `DETECTION-FCLONES-CACHE-IMPORT` | Opt-in import of full-file hashes from an existing upstream-`fclones` `--cache` database, so a tree fclones already scanned doesn't need re-hashing here | `DETECTION-INCREMENTAL-CACHE` | `FCLONE-DETECTION-001` (revision) | `cargo fmt`/`clippy`/`test`/`bench --no-run` green; real end-to-end verification against the actual `fclones` binary (not just hand-built fixtures) | Done | ADR-0019; `ScanOptions::fclones_import_path`/CLI `--import-fclones-cache <path>`, off by default, independent of `--cache`; new `fclones_import` module reads fclones' `sled`/`bincode` cache directly (reverse-engineered from fclones' own source, then verified byte-for-byte against a real `fclones group --cache --hash-fn xxhash` run); Unix only (fclones' Windows file-identity encoding isn't reproducible via the `file-id` crate) and only recovers fclones' two documented default prefix lengths for small files, not an explicit non-default `--max-prefix-size` — both documented, honest scope cuts, never a wrong result |
| `GUI` | Desktop GUI covering the same scan-and-act workflow as the CLI, reversing the v1 "no GUI" non-goal | `DETECTION-BASELINE`, `ACTION-LAYER` | `GUI-UX-001` | `cargo fmt`/`clippy`/`test` green (workspace, including the new crate); manual end-to-end verification of a real scan/preview/apply cycle through the rendered UI | Done | ADR-0020; new `rusty_fclone-gui` crate (Tauri v2, vanilla-JS frontend, no npm dependency); `start_scan`/`run_action` commands mirror `CLI-UX-001`'s NDJSON shape and ADR-0009's preview-by-default safety model; verified in this environment via Xvfb + `xdotool` (no real display available) — a full scan → duplicate-group render → preview → apply cycle confirmed against real filesystem state, not just IPC-level tests |
| `GUI-RELEASE-BUNDLES` | Packaged, installable GUI distribution (`.deb`/`.AppImage`/`.dmg`/`.msi`) via `tauri build`'s bundler, plus real (non-placeholder) application icon assets in every format the bundler needs | `GUI` | `GUI-UX-001` (revision) | `release.yml` (or a new workflow) produces installable GUI artifacts per platform, attached to a tagged release the same way `RELEASE-BINARIES` already does for the CLI binary | Not Started | `GUI` above ships the app itself; bundling needs per-platform prerequisites beyond CI's current build-and-test install step, plus real icon art — deliberately not attempted in the same change (see ADR-0020's consequences) |
| `DETECTION-FOLDER-DEDUP` | Detect folder-level duplicates — directories whose entire recursive content is identical to (`Exact`) or a subset of (`Contained`) another directory's — asked for directly by a real user after trying the GUI on their own machine | `DETECTION-BASELINE` | `FCLONE-DETECTION-001` (revision), `CLI-UX-001` (revision) | `cargo fmt`/`clippy`/`test`/`bench --no-run` green; manual CLI smoke test against a real multi-directory tree confirms both `Exact` and `Contained` output | Done | ADR-0021; new `rusty_fclone_core::folder_dedup` module and `find_folder_duplicates` function (post-scan, not a `scan()`/`ScanEvent` extension); CLI `--find-duplicate-folders`, off by default; GUI surfacing explicitly scoped out as a separate follow-up |
| `GUI-REDESIGN` | Rebuild `rusty_fclone-gui`'s bare-bones single-page frontend into a 4-screen app (Dashboard, Scan Setup, Duplicate Review, Rules & Automation) against a high-fidelity design handoff, wired to real backend data throughout (no mocks) | `GUI`, `DETECTION-FOLDER-DEDUP` | `GUI-UX-001` (revision) | `cargo fmt`/`clippy`/`test`/`bench --no-run`/`doc` green; manual Xvfb/`xdotool` end-to-end pass across all four screens, both themes, confirming real data and a real delete against the filesystem | Done | ADR-0022; new `find_duplicate_folders` Tauri command (landed first, its own small PR) surfaces `DETECTION-FOLDER-DEDUP` in the GUI for the first time; every mockup/reality gap (no folder-delete action, one scan root, fake toggles, Google Fonts dependency, fake OS window chrome, ...) resolved and recorded rather than silently papered over — see ADR-0022's Decision section for the full list |
| `FOLDER-ACTION` | A real action (delete/hardlink/reflink) for a `FolderMatch` — every file in one folder against its confirmed partner in another — closing the gap `DETECTION-FOLDER-DEDUP`/`GUI-REDESIGN` deliberately left open (the GUI's "Delete Duplicate Folder" button shipped disabled pending this decision) | `DETECTION-FOLDER-DEDUP` | `FCLONE-ACTION-001` (revision), `CLI-UX-001` (revision), `GUI-UX-001` (revision) | `cargo fmt`/`clippy`/`test`/`bench --no-run`/`doc` green; core capability has a dedicated test for every requirement | Done | ADR-0023; new `rusty_fclone_core::folder_action` module (`plan_folder`/`apply_folder`), reusing `action::apply` per file pair rather than new low-level mutation code, re-verifying every file's confirmed partner and current size before planning (fails closed on anything stale or unconfirmed); prunes the emptied folder only after a fully successful delete. `--find-duplicate-folders` combines with `--action`/`--apply` (`CLI-UX-001` 0.3.0, FR-013) — found and fixed a real ordering conflict between the CLI's live per-group action loop and the post-scan folder-dedup pass along the way. The GUI's "Delete Duplicate Folder" button is now enabled via a new `run_folder_action` command plus a per-folder keep-choice badge for `Exact` matches (`GUI-UX-001` 0.3.0, FR-018/FR-019) — verified against a real filesystem for both `Contained` and `Exact` matches, including the keep-choice actually driving which folder is removed |
| `DETECTION-SCAN-FILTERS` | Real include/exclude scan filters — min/max size, extension include/exclude, and excluded path subtrees — applied during traversal before any hashing; first unit of the phased gap-closure plan in `docs/roadmap/DEDUP-GAP-IMPLEMENTATION-PLAN.md` (competitive research found this near-universal among comparable tools and entirely absent here) | `DETECTION-BASELINE` | `FCLONE-DETECTION-001` (revision, FR-014/NFR-007), `CLI-UX-001` (revision, FR-014), `GUI-UX-001` (revision, FR-020) | `cargo fmt`/`clippy`/`test`/`bench --no-run`/`doc` green; new filter fields each have a dedicated unit test, including a real-traversal test proving an excluded directory subtree is never descended into | Done | New `ScanOptions` fields (`min_size`, `max_size`, `include_extensions`, `exclude_extensions`, `exclude_paths`), all defaulting to no filtering. `exclude_paths` prunes whole subtrees via jwalk's `process_read_dir` before descending, not a post-hoc filter — the exit gate's own emphasis, since a filter that still reads an excluded subtree's files defeats the point. CLI gained `--min-size`/`--max-size`/`--include-ext`/`--exclude-ext`/`--exclude-path` (repeatable); GUI's Scan Setup gained a real "Include/exclude filters" card wired to `ScanOptionsPayload`, deliberately left separate from the Rules & Automation screen's existing local-only-preview toggles rather than silently making only one of those three toggles real. No ADR — routine implementation, no architecture-level decision. |
| `ACTION-TRASH` | A recoverable `ActionKind::Trash` — moves a redundant copy to the OS trash/recycle bin instead of deleting it permanently; second unit of `docs/roadmap/DEDUP-GAP-IMPLEMENTATION-PLAN.md`'s Phase 1 (competitive research found Recycle Bin/Trash-routed deletion present in 7/8 comparable products, entirely absent here — `Delete` was always permanent) | `ACTION-LAYER` | `FCLONE-ACTION-001` (revision, FR-011 revised/FR-012), `GUI-UX-001` (revision, FR-008/FR-018 revised, FR-021) | `cargo fmt`/`clippy`/`test`/`bench --no-run`/`doc` green; a dedicated unit test per new behavior (core action, folder-action prune); real end-to-end confirmation the trashed file is actually recoverable, not just gone | Done | ADR-0024; new `trash` workspace dependency (`rusty_fclone-core`-only), same "dependency, not hand-rolled per-platform FFI" reasoning ADR-0014 used for reflink. `folder_action::apply_folder`'s directory-prune gate now fires for `Trash` as well as `Delete` (same post-condition: every file gone from `removed`). CLI gained `--action trash`; GUI's action selector gained a "Trash" option and now defaults to it instead of permanent `Delete`. Verified end-to-end in this environment (CLI file-level and folder-level smoke tests against real trees, confirmed via `ls`/`find` and the trashed file actually present and recoverable at `~/.local/share/Trash/files/`) — the GUI's new option is not yet manually verified through the rendered UI (no Xvfb/`xdotool` pass this session). |
| `SELECTION-RULES` | Bulk, rule-based keep selection — `Rule::{Newest, Oldest, ShortestPath, LongestPath}` alongside the existing alphabetically-first default, applied across every group in one pass with a one-line "why this one" reason; third and final unit of `docs/roadmap/DEDUP-GAP-IMPLEMENTATION-PLAN.md`'s Phase 1 (the playbook's cheap explainability win, and what makes the GUI's already-built Rules screen real instead of decorative) | `ACTION-LAYER` | `FCLONE-ACTION-001` (revision, FR-013/FR-014, reverses the "configurable keep-strategy" v1 non-goal), `CLI-UX-001` (revision, FR-015), `GUI-UX-001` (revision, FR-008/FR-009/FR-014 revised, FR-022) | `cargo fmt`/`clippy`/`test`/`bench --no-run`/`doc` green; a dedicated unit test per rule including tie-breaking behavior; manual CLI smoke test confirms one flag applies a rule across every group instead of per-group manual selection | Done | New `rusty_fclone_core::select` module (`choose_keep`); `action::plan` refactored into a thin wrapper over new `plan_with_keep`, which takes an explicit kept path. Deliberately no `Largest`/`Smallest` rule (every path in a `DuplicateGroup` shares the same size by definition) and no folder-level `FolderMatch::Exact` rule support (stays alphabetically-first) — both recorded as non-goals rather than silently out of scope. CLI gained `--keep-rule`; GUI's previously-fake "Keep newest copy" toggle is now real via a new `choose_keep` command, applied live to every group in Review with a manual keep-choice badge still taking precedence. No ADR — routine implementation, no architecture-level decision. Verified end-to-end via a real CLI smoke test (`--keep-rule newest` against a two-file tree with different mtimes, both text and JSON output showing the correct pick and reason) — the GUI's real toggle is not yet manually verified through the rendered UI (no Xvfb/`xdotool` pass this session, same standing gap as `DETECTION-SCAN-FILTERS`/`ACTION-TRASH`). |
| `ACTION-REFERENCE-FOLDERS` | Protected/reference-folder guardrail, as a hard block inside `plan`/`plan_folder` — a protected path is never placed in `actions`, not just flagged in the UI, and always wins as the kept path over any `Rule` or caller-chosen keep; first unit of `docs/roadmap/DEDUP-GAP-IMPLEMENTATION-PLAN.md`'s Phase 2 (independently built by 3 unrelated products per the playbook, the single strongest "build this" signal in that research) | `ACTION-LAYER`, `DETECTION-FOLDER-DEDUP` | `FCLONE-ACTION-001` (revision, FR-015 through FR-017), `CLI-UX-001` (revision, FR-016), `GUI-UX-001` (revision, FR-023) | `cargo fmt`/`clippy`/`test`/`bench --no-run`/`doc` green; a dedicated unit test proving a plan against a group containing a protected path omits that path and overrides the kept choice; manual test confirms the guardrail holds end-to-end against a real filesystem | Done | ADR-0025; new `reference_paths: &[PathBuf]` parameter on `select::choose_keep`, `action::plan`/`plan_with_keep`, and `folder_action::plan_folder` — a protected path always wins as `keep` (over every `Rule` and any caller-supplied `keep`) and is filtered out of `actions`/`pairs` independently, so the guarantee holds even for a caller that bypasses `choose_keep`. New `FolderActionPlan::protected_files_skipped` field; `apply_folder`'s directory-prune step now also requires it to be zero, closing a real gap found during implementation (pruning `removed` after every planned pair succeeds would otherwise still delete a protected file left inside it). CLI gained a repeatable `--reference <path>`; GUI gained a "Protected folders" field on Scan Setup, threaded into `run_action`/`choose_keep`/`run_folder_action` (not the detection-only commands), with the Review screen's rule-preview lookup now also resolving under the default alphabetical rule whenever a reference folder is configured. Verified end-to-end against real filesystems in this environment (CLI file-level and folder-level smoke tests: a protected file survived and was correctly reported as the keep reason despite alphabetical ordering favoring the other copy; a protected file inside a folder match's subset side survived and the directory was not pruned) — the GUI's new field is not yet manually verified through the rendered UI (`xdotool` isn't installed in this environment, same standing gap as `DETECTION-SCAN-FILTERS`/`ACTION-TRASH`/`SELECTION-RULES`). |
| `ACTION-MOVE-COPY` | `ActionKind::Move`/`ActionKind::Copy` to a user-chosen archive folder, alongside delete/hardlink/reflink/trash — `Move` relocates a redundant copy into the archive (space reclaimed at the scanned location, same as delete/trash/hardlink/reflink); `Copy` archives it while leaving the original untouched (reclaims nothing, a consolidate-for-review step); second unit of `docs/roadmap/DEDUP-GAP-IMPLEMENTATION-PLAN.md`'s Phase 2 | `ACTION-LAYER` | `FCLONE-ACTION-001` (revision, FR-018/FR-019, reverses the "moving files ... not implemented" v1 non-goal), `CLI-UX-001` (revision, FR-017), `GUI-UX-001` (revision, FR-024) | `cargo fmt`/`clippy`/`test`/`bench --no-run`/`doc` green; a dedicated unit test per new behavior (core action, folder-action prune gate extended to `Move`); manual test confirms a moved file's new path is correct and the original disappears, and that `Copy` leaves the original untouched | Done | ADR-0026; `ActionKind::Move(PathBuf)`/`Copy(PathBuf)` carry their archive destination as data on the variant itself rather than a new threaded parameter — `ActionKind` no longer derives `Copy` (the trait) as a result, with every call site relying on that implicit copy updated to clone or borrow explicitly instead. Archived paths mirror the original path's structure underneath the archive directory (collision-safe across files with the same name from different original directories); a destination that already exists is always a per-file failure, never a silent overwrite. `Move` never falls back to copy-then-remove on a cross-device `rename` failure, matching ADR-0014's own "fail loud on a filesystem limitation" choice for reflink. `folder_action::apply_folder`'s directory-prune gate now also fires for `Move`. CLI gained `--action move`/`copy` plus a required `--archive-dir <path>`; GUI gained a conditional "Archive folder" field in the Review action bar, with `Copy`'s reclaim-estimate text rewritten to avoid showing a byte figure that would never actually be freed. Verified end-to-end against a real filesystem in this environment (`move` relocated a redundant file to its mirrored archive path; `copy` archived a file while leaving both originals in place with zero bytes reclaimed; a repeated `copy` run against the same tree failed cleanly on the already-archived destination rather than clobbering it; `move` without `--archive-dir` was rejected before any scan ran) — the GUI's new field is not yet manually verified through the rendered UI (same standing gap as `ACTION-REFERENCE-FOLDERS`). |
| `CLI-HISTORY-AUDIT` | Closes the two gaps `CLI-SCAN-HISTORY` deliberately deferred: (1) per-action detail rows (file/pair, kind, bytes, success/failure) alongside the existing scan-summary row, and (2) a `history` query/report subcommand (list recent scans, aggregate totals over a date range). GUI's disabled "Export (JSON)" button gets wired to real backend data; "Import history" stays honestly deferred pending a separate prerequisite. Third and final unit of `docs/roadmap/DEDUP-GAP-IMPLEMENTATION-PLAN.md`'s Phase 2, completing the phase | `CLI-SCAN-HISTORY` | `CLI-UX-001` (revision, FR-018), `GUI-UX-001` (revision, FR-025) | `cargo fmt`/`clippy`/`test`/`bench --no-run`/`doc` green; new rows/queries covered by unit tests mirroring the existing `history` tests; manual test: two scans plus a query round-trip through the new subcommand match what actually happened on disk | Done | ADR-0027; new `actions` SQLite table (one row per file/pair an applied action actually acted on, FK'd to its `scans` row, written in the same transaction) — scoped to applied actions only, since a preview runs nothing real to audit. `history list`/`stats` are new read-only queries; `rusty-fclone history <SUBCOMMAND>` is a separate top-level command, reached via a manual `args[1] == "history"` pre-dispatch in `main` rather than folding `history` into `Cli` as a `#[command(subcommand)]` — `Cli::root`'s required positional argument makes that combination ambiguous for clap without restructuring every existing scan invocation, a breaking change this unit didn't call for. `--since`/`--until` take raw Unix timestamps rather than adding a date-parsing dependency. GUI's "Export (JSON)" now downloads the session's in-memory scan history via a plain `<a download>`/object-URL — a real webview download needing no new Tauri plugin; "Import history" stays an explicit disabled placeholder (reading an arbitrary database needs a real filesystem path, which needs the same Tauri `dialog`/`fs` plugin work this project already tracks as a separate prerequisite for the GUI's root-path field). Verified end-to-end against a real filesystem in this environment: two real scans (one report-only, one `--action trash --apply`) followed by `history list` (both output formats) and `history stats` matched the real per-scan and per-action outcomes exactly; a plain `rusty-fclone <ROOT>` invocation confirmed unaffected by the new keyword dispatch. |

## Known gaps carried from the v1 baseline (not blocking, but tracked)

None currently open — see "Closed" below. Path storage still uses a plain
`HashMap`-based model rather than fclones-style prefix-compressed storage,
but that's a deliberate, standing design choice (ADR-0004), not a tracked
gap: it's explicitly conditioned on benchmark evidence from a real
multi-million-file tree showing it's the actual bottleneck, which doesn't
exist yet. The redundant-clone cost that *was* a genuine, unconditional gap
is closed (see below).

Closed: cycle-detection test for `--follow-symlinks`
(`traversal::tests::follow_symlinks_terminates_on_a_cycle`, confirms jwalk's
loop detection actually works, with a bounded timeout so a regression fails
loudly instead of hanging); full-file hashing and `--verify` now stream in
fixed-size chunks instead of buffering whole files (ADR-0002 addendum);
structured logging/observability via `tracing` spans/events on the
traversal and pipeline stages, with a CLI `-v`/`--verbose` flag and
`RUST_LOG` support (ADR-0010); redundant path-clone cost in the detection
pipeline, by switching internal path storage from `PathBuf` to `Arc<Path>`
so cloning a path through the grouping stages is a refcount bump instead of
a fresh allocation and copy (ADR-0011).
